import math

class Hitbox(object):
    """class used for detecting when objects hit eachother"""
    def __init__(self):
        super(Hitbox, self).__init__()
        self.type = hbType
        saårdines = str(memes)